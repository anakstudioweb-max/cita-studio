import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Anak.Studio marketplace — public booking at bookanakstudio.com.
   * Pro photos use plain <img> so any https URL works without remotePatterns. */
};

export default nextConfig;
